<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.8</version>
        <relativePath/>
    </parent>

    <groupId>com.im</groupId>
    <artifactId>im-parent</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>
    <name>im-parent</name>
    <description>IM 即时通讯系统 - 多模块父工程（Spring Boot 4 + JDK 21）</description>

    <modules>
        <module>im-common</module>
        <module>im-user</module>
        <module>im-friend</module>
        <module>im-conversation</module>
        <module>im-message</module>
        <module>im-group</module>
        <module>im-file</module>
        <module>im-websocket</module>
        <module>im-ai</module>
        <module>im-remote</module>
        <module>im-bootstrap</module>
        <!-- 被控端 Agent：独立 fat jar，不被 bootstrap 依赖 -->
        <module>im-remote-agent</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <!-- 与 parent 保持一致，供 annotationProcessorPaths 显式引用 -->
        <spring-boot.version>4.0.8</spring-boot.version>

        <!-- 内部模块版本 -->
        <im.version>1.0.0</im.version>

        <!-- 持久层：Spring Boot 4 专用 starter，分页插件在 3.5.9+ 已拆分需显式引入 -->
        <mybatis-plus.version>3.5.17</mybatis-plus.version>

        <!-- 鉴权：1.45.0 起提供 sa-token-spring-boot4-starter -->
        <sa-token.version>1.46.0</sa-token.version>

        <!-- 接口文档：Knife4j 官方停更于 4.5.0，Boot 4 使用维护分支 knife4j-next -->
        <knife4j.version>5.6.0</knife4j.version>

        <!-- 对象存储 -->
        <minio.version>8.6.0</minio.version>

        <!--
          OkHttp：minio 8.6.0 直接依赖 com.squareup.okhttp3:okhttp:5.1.0，
          但 5.x 起 okhttp 改成了 Gradle 多平台产物，Maven 仓库里那个 jar 只有几百字节、
          不含任何 class（POM 里还标着 do_not_remove: published-with-gradle-metadata），
          真正的 JVM 实现被拆到了 okhttp-jvm。纯 Maven 构建必须显式补上，
          否则编译期报 cannot access okhttp3.HttpUrl，运行期直接 NoClassDefFoundError。
          版本与 minio 自己声明的保持一致，免得两套 okhttp 同时进 classpath。
        -->
        <okhttp.version>5.1.0</okhttp.version>

        <!-- JWT 工具（WS 连接票据 / 文件访问票据） -->
        <hutool.version>5.8.47</hutool.version>

        <!-- 密码加密：Argon2 / BCrypt -->
        <spring-security-crypto.version>7.0.7</spring-security-crypto.version>
        <bouncycastle.version>1.86</bouncycastle.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- ==================== 内部模块 ==================== -->
            <dependency>
                <groupId>com.im</groupId>
                <artifactId>im-common</artifactId>
                <version>${im.version}</version>
            </dependency>
            <dependency>
                <groupId>com.im</groupId>
                <artifactId>im-user</artifactId>
                <version>${im.version}</version>
            </dependency>
            <dependency>
                <groupId>com.im</groupId>
                <artifactId>im-friend</artifactId>
                <version>${im.version}</version>
            </dependency>
            <dependency>
                <groupId>com.im</groupId>
                <artifactId>im-conversation</artifactId>
                <version>${im.version}</version>
            </dependency>
            <dependency>
                <groupId>com.im</groupId>
                <artifactId>im-message</artifactId>
                <version>${im.version}</version>
            </dependency>
            <dependency>
                <groupId>com.im</groupId>
                <artifactId>im-group</artifactId>
                <version>${im.version}</version>
            </dependency>
            <dependency>
                <groupId>com.im</groupId>
                <artifactId>im-file</artifactId>
                <version>${im.version}</version>
            </dependency>
            <dependency>
                <groupId>com.im</groupId>
                <artifactId>im-websocket</artifactId>
                <version>${im.version}</version>
            </dependency>
            <dependency>
                <groupId>com.im</groupId>
                <artifactId>im-ai</artifactId>
                <version>${im.version}</version>
            </dependency>
            <dependency>
                <groupId>com.im</groupId>
                <artifactId>im-remote</artifactId>
                <version>${im.version}</version>
            </dependency>

            <!-- ==================== 持久层 ==================== -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot4-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-jsqlparser</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>

            <!-- ==================== 鉴权 ==================== -->
            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-spring-boot4-starter</artifactId>
                <version>${sa-token.version}</version>
            </dependency>
            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-jwt</artifactId>
                <version>${sa-token.version}</version>
            </dependency>
            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-redis-template</artifactId>
                <version>${sa-token.version}</version>
            </dependency>

            <!-- ==================== 接口文档 ==================== -->
            <dependency>
                <groupId>com.baizhukui</groupId>
                <artifactId>knife4j-openapi3-boot4-spring-boot-starter</artifactId>
                <version>${knife4j.version}</version>
            </dependency>

            <!-- ==================== 对象存储 ==================== -->
            <dependency>
                <groupId>io.minio</groupId>
                <artifactId>minio</artifactId>
                <version>${minio.version}</version>
            </dependency>
            <dependency>
                <groupId>com.squareup.okhttp3</groupId>
                <artifactId>okhttp-jvm</artifactId>
                <version>${okhttp.version}</version>
            </dependency>

            <!-- ==================== 工具 ==================== -->
            <dependency>
                <groupId>cn.hutool</groupId>
                <artifactId>hutool-jwt</artifactId>
                <version>${hutool.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.security</groupId>
                <artifactId>spring-security-crypto</artifactId>
                <version>${spring-security-crypto.version}</version>
            </dependency>
            <dependency>
                <groupId>org.bouncycastle</groupId>
                <artifactId>bcprov-jdk18on</artifactId>
                <version>${bouncycastle.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!--
      全部子模块共享的依赖。
      Lombok 是 provided，不会随 im-common 传递给其他模块，若只声明在 im-common 中，
      下游模块会出现「package lombok does not exist」，因此必须在父 POM 统一声明。
    -->
    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <release>${maven.compiler.release}</release>
                        <encoding>UTF-8</encoding>
                        <!-- 保留方法参数名，Spring MVC / MyBatis 依赖 -->
                        <parameters>true</parameters>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                            <path>
                                <groupId>org.springframework.boot</groupId>
                                <artifactId>spring-boot-configuration-processor</artifactId>
                                <version>${spring-boot.version}</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <configuration>
                        <skipTests>true</skipTests>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
