1. 腾讯TRTC 功能 腾讯防水墙（人机验证） 事务邮箱 手机验证码登录 有了服务器在开发
2. 日志系统
3.Tauri2
打包安卓 重新打包exe



1. 账号认证体系
- 登录方式：账号密码登录、邮箱登录、手机验证码登录。
- 密码找回：邮箱找回、手机验证码找回。
- 登录设备管理：查看登录设备、踢下线；支持单点登录/多端互踢。
- 登录日志：记录每次登录信息。
- 验证码策略：频率限制（防短信 / 邮箱轰炸）、验证码有效期、验证码复用禁止。
- 密码策略：密码复杂度配置、密码历史记录，禁止重复使用旧密码。
- 敏感操作二次验证：修改密码、解绑邮箱 / 手机、踢设备下线，需要二次验证码 / 人机验证。
- Token 设计：区分**access_token（短期）+ refresh_token（长期）**；refresh_token 在踢下线时一并失效。
- 单点登录细节：同一账号可配置两种模式：①多端互踢（只允许 1 端在线）②允许多端在线（仅支持单独踢指定设备）。
- 登录日志字段：IP、设备 UA、地理位置、登录结果（成功 / 失败）、操作时间、登录类型。
2. 远程电脑控制功能
- 权限模型：被控端授权机制，**必须被控方同意才能发起远程**；可设置只读 / 可操作权限。
- 流压缩：屏幕画面编码压缩，带宽自适应调节；键鼠事件可靠传输。
- 安全鉴权：远程会话独立 token，一次性有效；会话超时自动断开。
- 会话审计：远程会话日志，记录开始 / 结束时间、操作双方；可选录屏审计。
- NAT 穿透 fallback：P2P 优先，P2P 不通走服务端中继；中继带宽限流。
- 被控端状态：离线、忙、拒绝接入。

3. 安全防护功能（已完成，见 md/安全防护功能测试文档.md）
1. XSS/CSRF 防护：聊天消息渲染转义，防止 XSS；接口 CSRF Token。✔ 渲染层插值转义 + 安全响应头；凭证 header-only（is-read-cookie:false）从机制上消除 CSRF
2. 接口鉴权：所有 API 统一鉴权，防止越权访问别人视频、消息、文件。✔ Sa-Token 全局拦截 + 会话成员/文件访问权逐项校验
3. 限流：登录接口、上传接口、消息发送接口、远程控制接口，全接口限流防刷。✔ @RateLimit 注解 + Redis 计数 + 全局按 IP 兜底 + WS 通道限流（远程控制接口尚未开发，开发时标注解即可纳入）

4. AI-RAG（已完成：左侧控制栏「面试」入口 → 后端 Java 全栈面试页面）
✔ 新增 im-ai 模块：知识库 RAG（BM25 检索，目录 D:/资料/知识库/面试官 可配置 im.ai.knowledge-dir，增删改文件自动重建索引无需重启）
✔ 模型接入：阿里云百炼 DashScope compatible-mode（OpenAI 协议，JDK HttpClient 直连 SSE 流式），配置在 spring.ai.openai（base-url/api-key=ALI_BABA_API_KEY/model=AI_MODEL_NAME 默认 qwen-flash/temperature 0.7/timeout 60s）
✔ 预设模板已改为「后端 Java 全栈面试」（InterviewService.INTERVIEWER_PERSONA）：考察 Java/JVM、并发、Spring、MySQL、Redis、分布式、安全与全栈协作；下面这段规则保留作方法论参考（循序渐进/每次只提1问/追问薄弱点/不直接给答案/打分总结），模板调整以代码里的系统提示词为准
✔ 接口：POST /api/ai/interview/chat（SSE 流式，限流 20 次/分/用户）、GET /api/ai/interview/status（知识库/模型概况）；无数据库变更，重启后端即生效
你现在是后端/全栈高级面试官，基于上面知识库进行面试。
规则：
1. 你只能基于知识库内容提问，不要凭空增加需求；可以深挖候选人给出方案里的漏洞。
2. 面试采用循序渐进方式：先让候选人做整体架构设计，然后分模块深挖，每次只提1个问题，不要一次性抛出一堆问题。
3. 候选人回答后，评估回答优缺点，针对薄弱点继续追问；如果候选人方案有风险点，指出来并继续深入。
4. 面试考察维度：架构选型、存储设计、并发、安全、边界异常、性能、落地坑点。
5. 禁止直接给完整答案。
6. 当候选人问需求细节，可以从知识库提取信息回答，但不要主动扩展额外需求。
7. 面试流程：
    阶段1：请候选人简述整套系统整体架构，模块拆分，技术选型。
    阶段2：逐个模块深挖：视频转码上传下载、IM消息、本地SQLite缓存、登录认证多端互踢、关键词屏蔽、文件预览白名单、远程控制。
    阶段3：深挖难点：浏览器SQLite每个用户独立DB怎么实现、存储配额问题；消息已读/未读红点；多清晰度视频转码任务队列；多端互踢session设计；动态关键词热更新；黑名单消息拦截时机；腾讯防水墙接入位置。
    阶段4：异常场景、安全风险、性能瓶颈、灾备。
8. 面试结束后，可以对候选人整体方案打分并总结。

5. AI-RAG  腾讯TRTC

6.实现缓存功能 前端缓存 后端缓存






