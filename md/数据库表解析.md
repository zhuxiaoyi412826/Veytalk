# IM 即时通讯系统 · 数据库表解析

> 事实来源：`sql/im_schema.sql`（建库建表）、`sql/im_data.sql`（种子数据）、`im-common` 的 `MessageExtra`。
> 适用环境：MySQL 8.0+ / InnoDB / utf8mb4。库名 `im_db`，共 **15 张表**，分属 **6 个业务模块**。

---

## 目录

- [一、全局设计约定](#一全局设计约定)
- [二、表清单总览](#二表清单总览)
- [三、实体关系（ER）概览](#三实体关系er概览)
- [四、用户与权限模块（im-user）](#四用户与权限模块im-user)
- [五、好友关系模块（im-friend）](#五好友关系模块im-friend)
- [六、会话模块（im-conversation）](#六会话模块im-conversation)
- [七、消息模块（im-message）](#七消息模块im-message)
- [八、群组模块（im-group）](#八群组模块im-group)
- [九、文件模块（im-file）](#九文件模块im-file)
- [十、扩展说明](#十扩展说明)

---

## 一、全局设计约定

所有表共享以下几条贯穿全库的约定，理解它们后单表就只剩业务差异：

| 约定 | 说明 |
|---|---|
| **主键 BIGINT + 雪花算法** | 所有主键为 `BIGINT`，由 MyBatis-Plus 雪花算法（Snowflake）在应用侧生成，**不使用数据库自增**。目的是便于将来分库分表，且 ID 全局趋势递增、可暴露粗略的时间先后。 |
| **逻辑删除 `deleted`** | 只对「需要保留历史的主表」启用（用户、角色、权限、会话、群组、文件、消息主表除外）。取值 `0` 未删除 / `1` 已删除。MyBatis-Plus 自动在查询上追加 `deleted=0`。 |
| **关系表物理删除** | 好友、群成员、会话成员等**关系表不启用逻辑删除**——因为唯一键若保留已删除行，会导致「删了好友/退了群却无法重新添加」。群成员是例外，用 `status` 软状态保留历史（见下文）。 |
| **时间列 DATETIME** | `create_time` / `update_time` 由 MyBatis-Plus 自动填充，同时数据库给出 `DEFAULT CURRENT_TIMESTAMP` 与 `ON UPDATE CURRENT_TIMESTAMP` 兜底，即使绕过应用直接写库也有正确时间。 |
| **字符集 utf8mb4** | 全库统一 `utf8mb4` / `utf8mb4_general_ci`，完整支持 emoji 与生僻字。 |
| **状态位 TINYINT** | 各类 `status` / 布尔标记统一用 `TINYINT`（0/1/2…），比 `BOOLEAN` 更利于表达多态状态。 |

---

## 二、表清单总览

| # | 表名 | 中文含义 | 所属模块 | 逻辑删除 | 核心唯一键 |
|---|---|---|---|---|---|
| 1 | `im_user` | 用户表 | im-user | ✅ | `uk_username` |
| 2 | `im_role` | 角色表 | im-user | ✅ | `uk_role_code` |
| 3 | `im_permission` | 权限表 | im-user | ✅ | `uk_perm_code` |
| 4 | `im_user_role` | 用户-角色关联表 | im-user | ❌ | `uk_user_role` |
| 5 | `im_role_permission` | 角色-权限关联表 | im-user | ❌ | `uk_role_perm` |
| 6 | `im_friend` | 好友关系表（双向各一行） | im-friend | ❌ | `uk_user_friend` |
| 7 | `im_friend_request` | 好友申请表 | im-friend | ❌ | `uk_from_to_pending` |
| 8 | `im_conversation` | 会话表 | im-conversation | ✅ | `uk_biz_key` |
| 9 | `im_conversation_member` | 会话成员表（用户维度会话状态） | im-conversation | ❌（`is_deleted` 本端隐藏） | `uk_conv_user` |
| 10 | `im_message` | 消息表（兼离线消息队列） | im-message | ❌（用 `is_recalled`） | `uk_from_client` |
| 11 | `im_message_read` | 消息送达/已读回执表 | im-message | ❌ | `uk_msg_user` |
| 12 | `im_message_delete` | 消息单端删除表 | im-message | ❌ | `uk_msg_user` |
| 13 | `im_group` | 群组表 | im-group | ✅ | —（`idx_owner` 普通索引） |
| 14 | `im_group_member` | 群成员表 | im-group | ❌（`status` 软状态） | `uk_group_user` |
| 15 | `im_file` | 文件元数据表 | im-file | ✅ | —（`idx_md5` 普通索引） |

---

## 三、实体关系（ER）概览

系统未建物理外键（`FOREIGN_KEY_CHECKS=0` 便于重建脚本），关系靠应用层与索引维护。逻辑关系如下：

```
im_user ──< im_user_role >── im_role ──< im_role_permission >── im_permission
   │
   ├──< im_friend >──（自关联：user_id ↔ friend_id，双向两行）
   ├──< im_friend_request（from_user_id / to_user_id）
   │
   ├──< im_conversation_member >── im_conversation ──< im_message
   │                                      │                  ├──< im_message_read
   │                                      │                  └──< im_message_delete
   │                                      └──（type=2 群聊）── im_group ──< im_group_member
   │
   └──< im_file（uploader_id）
```

**权限模型**：经典 RBAC——`用户 →(im_user_role)→ 角色 →(im_role_permission)→ 权限点`。一个用户可有多角色，一个角色可有多权限点，多对多通过两张关联表落地。

**会话模型**：`im_conversation` 是「一个聊天窗口」的抽象（单聊/群聊统一），`im_conversation_member` 是「每个用户在该会话里的私有状态」（未读数、置顶、免打扰、本端是否隐藏）。二者一对多。

---

## 四、用户与权限模块（im-user）

### 4.1 `im_user` — 用户表

系统的账号主体，一行一个用户，承载登录凭证与个人资料。

| 字段 | 类型 | 允许空 | 默认值 | 含义 |
|---|---|---|---|---|
| `id` | BIGINT | 否 | — | 用户 ID（雪花） |
| `username` | VARCHAR(32) | 否 | — | 登录账号，全局唯一 |
| `password` | VARCHAR(255) | 否 | — | 密码密文（Argon2id / BCrypt），**永不明文存储** |
| `nickname` | VARCHAR(32) | 否 | — | 昵称 |
| `avatar` | VARCHAR(512) | 是 | NULL | 头像访问地址 |
| `gender` | TINYINT | 否 | 0 | 性别：0 未知 / 1 男 / 2 女 |
| `signature` | VARCHAR(255) | 是 | NULL | 个性签名 |
| `phone` | VARCHAR(20) | 是 | NULL | 手机号，可空但**非空时唯一** |
| `email` | VARCHAR(64) | 是 | NULL | 邮箱 |
| `status` | TINYINT | 否 | 1 | 状态：1 正常 / 0 禁用 |
| `last_login_time` | DATETIME | 是 | NULL | 最近登录时间 |
| `last_login_ip` | VARCHAR(64) | 是 | NULL | 最近登录 IP |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |
| `deleted` | TINYINT | 否 | 0 | 逻辑删除：0 未删除 / 1 已删除 |

**索引：**

| 索引名 | 类型 | 列 | 用途 |
|---|---|---|---|
| `PRIMARY` | 主键 | `id` | 主键定位 |
| `uk_username` | 唯一 | `username` | 保证登录账号全局唯一，登录/注册查重直接命中 |
| `uk_phone` | 唯一 | `phone` | 手机号唯一。MySQL 唯一索引允许多个 NULL，故未填手机号的用户不受约束 |
| `idx_nickname` | 普通 | `nickname` | 支持按昵称模糊搜索用户（加好友场景） |

### 4.2 `im_role` — 角色表

RBAC 的角色定义。种子数据内置 `admin`（超级管理员）与 `user`（普通用户，注册即默认绑定）两种。

| 字段 | 类型 | 允许空 | 默认值 | 含义 |
|---|---|---|---|---|
| `id` | BIGINT | 否 | — | 角色 ID |
| `role_code` | VARCHAR(64) | 否 | — | 角色编码，如 `admin` / `user` |
| `role_name` | VARCHAR(64) | 否 | — | 角色名称 |
| `description` | VARCHAR(255) | 是 | NULL | 角色说明 |
| `status` | TINYINT | 否 | 1 | 状态：1 启用 / 0 停用 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |
| `deleted` | TINYINT | 否 | 0 | 逻辑删除 |

**索引：**

| 索引名 | 类型 | 列 | 用途 |
|---|---|---|---|
| `PRIMARY` | 主键 | `id` | 主键定位 |
| `uk_role_code` | 唯一 | `role_code` | 角色编码唯一，Sa-Token 按 code 加载角色 |

### 4.3 `im_permission` — 权限表

细粒度权限点定义，与代码里 `com.im.common.constant.ImConstants` 的常量一一对应，被 `@SaCheckPermission` 注解消费。

| 字段 | 类型 | 允许空 | 默认值 | 含义 |
|---|---|---|---|---|
| `id` | BIGINT | 否 | — | 权限 ID |
| `perm_code` | VARCHAR(128) | 否 | — | 权限标识，如 `message:send` |
| `perm_name` | VARCHAR(64) | 否 | — | 权限名称 |
| `module` | VARCHAR(32) | 是 | NULL | 所属模块：user/friend/message/group/file/system |
| `description` | VARCHAR(255) | 是 | NULL | 权限说明 |
| `status` | TINYINT | 否 | 1 | 状态：1 启用 / 0 停用 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |
| `deleted` | TINYINT | 否 | 0 | 逻辑删除 |

**索引：**

| 索引名 | 类型 | 列 | 用途 |
|---|---|---|---|
| `PRIMARY` | 主键 | `id` | 主键定位 |
| `uk_perm_code` | 唯一 | `perm_code` | 权限标识唯一，鉴权按 code 匹配 |
| `idx_module` | 普通 | `module` | 按模块聚合权限点（后台权限管理页分组展示） |

**内置 7 个权限点（种子数据）：**

| perm_code | 名称 | 模块 | admin | user |
|---|---|---|:---:|:---:|
| `user:profile:update` | 修改个人资料 | user | ✅ | ✅ |
| `friend:apply` | 发起好友申请 | friend | ✅ | ✅ |
| `message:send` | 发送消息 | message | ✅ | ✅ |
| `message:recall:any` | 撤回任意消息 | message | ✅ | ❌ |
| `group:create` | 创建群组 | group | ✅ | ✅ |
| `file:upload` | 上传文件 | file | ✅ | ✅ |
| `system:manage` | 系统管理 | system | ✅ | ❌ |

> `system:manage` 目前仅 admin 持有，是预留给后台管理的权限点，暂无接口消费。

### 4.4 `im_user_role` — 用户角色关联表

用户与角色的多对多关联。物理删除（无 `deleted`）。

| 字段 | 类型 | 允许空 | 默认值 | 含义 |
|---|---|---|---|---|
| `id` | BIGINT | 否 | — | 主键 |
| `user_id` | BIGINT | 否 | — | 用户 ID |
| `role_id` | BIGINT | 否 | — | 角色 ID |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP | 创建时间 |

**索引：**

| 索引名 | 类型 | 列 | 用途 |
|---|---|---|---|
| `PRIMARY` | 主键 | `id` | 主键定位 |
| `uk_user_role` | 唯一 | `user_id, role_id` | 防止同一用户重复绑定同一角色 |
| `idx_role` | 普通 | `role_id` | 反查「某角色下有哪些用户」 |

### 4.5 `im_role_permission` — 角色权限关联表

角色与权限点的多对多关联。物理删除（无 `deleted`）。

| 字段 | 类型 | 允许空 | 默认值 | 含义 |
|---|---|---|---|---|
| `id` | BIGINT | 否 | — | 主键 |
| `role_id` | BIGINT | 否 | — | 角色 ID |
| `permission_id` | BIGINT | 否 | — | 权限 ID |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP | 创建时间 |

**索引：**

| 索引名 | 类型 | 列 | 用途 |
|---|---|---|---|
| `PRIMARY` | 主键 | `id` | 主键定位 |
| `uk_role_perm` | 唯一 | `role_id, permission_id` | 防止同一角色重复绑定同一权限点 |
| `idx_permission` | 普通 | `permission_id` | 反查「某权限点被授予了哪些角色」 |

---

## 五、好友关系模块（im-friend）

### 5.1 `im_friend` — 好友关系表（双向各一行）

好友关系用**双向冗余两行**表达：A 加 B 为好友，会同时插入 `(A→B)` 与 `(B→A)` 两行。这样每一方都能独立维护自己的备注名、分组、拉黑状态，互不影响。

| 字段 | 类型 | 允许空 | 默认值 | 含义 |
|---|---|---|---|---|
| `id` | BIGINT | 否 | — | 主键 |
| `user_id` | BIGINT | 否 | — | 用户 ID（本端视角的「我」） |
| `friend_id` | BIGINT | 否 | — | 好友用户 ID（本端视角的「对方」） |
| `remark` | VARCHAR(32) | 是 | NULL | 我对好友的备注名 |
| `group_name` | VARCHAR(32) | 否 | `默认分组` | 好友分组名 |
| `status` | TINYINT | 否 | 1 | 状态：1 正常 / 2 已拉黑（我拉黑了 friend_id） |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP | 建立时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

**索引：**

| 索引名 | 类型 | 列 | 用途 |
|---|---|---|---|
| `PRIMARY` | 主键 | `id` | 主键定位 |
| `uk_user_friend` | 唯一 | `user_id, friend_id` | 保证同一方向不会重复建立好友关系 |
| `idx_friend` | 普通 | `friend_id` | 反查「谁把我加为好友」 |

> **为何拉黑是单向 `status=2` 而非删行**：拉黑只影响本端（我不接收对方消息），对方视角的关系仍在，故用状态位而非物理删除，避免破坏双向对称性。

### 5.2 `im_friend_request` — 好友申请表

好友申请的全生命周期记录（待处理/已同意/已拒绝/已过期）。**保留全部历史**，不做逻辑删除。

| 字段 | 类型 | 允许空 | 默认值 | 含义 |
|---|---|---|---|---|
| `id` | BIGINT | 否 | — | 主键 |
| `from_user_id` | BIGINT | 否 | — | 申请人 ID |
| `to_user_id` | BIGINT | 否 | — | 被申请人 ID |
| `verify_message` | VARCHAR(255) | 是 | NULL | 验证消息（申请附言） |
| `source` | VARCHAR(32) | 是 | NULL | 来源：search / qrcode / group |
| `status` | TINYINT | 否 | 0 | 状态：0 待处理 / 1 已同意 / 2 已拒绝 / 3 已过期 |
| `handle_time` | DATETIME | 是 | NULL | 处理时间 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP | 申请时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |
| `pending_flag` | TINYINT **（生成列 VIRTUAL）** | — | 由 `status` 计算 | 待处理标记：`status=0` 时为 1，否则为 NULL |

**索引：**

| 索引名 | 类型 | 列 | 用途 |
|---|---|---|---|
| `PRIMARY` | 主键 | `id` | 主键定位 |
| `uk_from_to_pending` | 唯一 | `from_user_id, to_user_id, pending_flag` | **精妙点**：见下方说明 |
| `idx_to_status` | 普通 | `to_user_id, status, create_time` | 「我收到的申请」列表：按被申请方过滤 + 状态 + 时间排序 |
| `idx_from_status` | 普通 | `from_user_id, status, create_time` | 「我发出的申请」列表：按申请方过滤 + 状态 + 时间排序 |

> **`pending_flag` 生成列 + 唯一索引的巧思**：需求是「同一对用户最多只能有一条**待处理**申请，但要保留所有历史记录」。直接把 `(from_user_id, to_user_id)` 设为唯一键会导致历史记录无法留存。方案是引入虚拟生成列 `pending_flag = IF(status=0, 1, NULL)`：待处理时为 1，处理后为 NULL。**MySQL 唯一索引允许多个 NULL 值**，因此：
> - 待处理时 `(A, B, 1)` 唯一 → 同一对用户第二条待处理申请插入失败；
> - 处理后 `pending_flag` 变 NULL → `(A, B, NULL)` 不触发唯一约束，可无限保留历史。

---

## 六、会话模块（im-conversation）

### 6.1 `im_conversation` — 会话表

一个「聊天窗口」的抽象，单聊与群聊统一建模。冗余了最后一条消息的摘要，使会话列表页无需 join 消息表即可渲染。

| 字段 | 类型 | 允许空 | 默认值 | 含义 |
|---|---|---|---|---|
| `id` | BIGINT | 否 | — | 会话 ID |
| `type` | TINYINT | 否 | — | 类型：1 单聊 / 2 群聊 |
| `target_id` | BIGINT | 是 | NULL | 群聊为群 ID；单聊为 NULL（对方通过 `im_conversation_member` 解析） |
| `biz_key` | VARCHAR(64) | 否 | — | 业务唯一键：单聊 `s:{小ID}:{大ID}`，群聊 `g:{groupId}` |
| `last_msg_id` | BIGINT | 是 | NULL | 最后一条消息 ID |
| `last_msg_content` | VARCHAR(512) | 是 | NULL | 最后一条消息摘要 |
| `last_msg_type` | TINYINT | 是 | NULL | 最后一条消息类型 |
| `last_msg_time` | DATETIME | 是 | NULL | 最后一条消息时间，会话列表排序依据 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |
| `deleted` | TINYINT | 否 | 0 | 逻辑删除 |

**索引：**

| 索引名 | 类型 | 列 | 用途 |
|---|---|---|---|
| `PRIMARY` | 主键 | `id` | 主键定位 |
| `uk_biz_key` | 唯一 | `biz_key` | 保证同一对用户/同一个群只存在一个会话，是 `getOrCreate` 幂等的基础 |
| `idx_last_msg_time` | 普通 | `last_msg_time` | 会话列表按最新消息时间排序 |

> **`biz_key` 排序约定**：单聊 key 为 `s:{较小用户ID}:{较大用户ID}`，把两个用户 ID 排序后拼接，保证 A→B 与 B→A 生成**完全相同**的 key，从而唯一索引能拦住重复会话。

### 6.2 `im_conversation_member` — 会话成员表（用户维度会话状态）

每个用户在每个会话里的**私有状态**。一个单聊会话有 2 行，群聊有 N 行。这里的 `is_deleted` 是「本端隐藏会话」，与全局逻辑删除语义不同。

| 字段 | 类型 | 允许空 | 默认值 | 含义 |
|---|---|---|---|---|
| `id` | BIGINT | 否 | — | 主键 |
| `conversation_id` | BIGINT | 否 | — | 会话 ID |
| `user_id` | BIGINT | 否 | — | 成员用户 ID |
| `unread_count` | INT | 否 | 0 | 未读消息数 |
| `last_ack_seq` | BIGINT | 否 | 0 | 已确认读到的消息 seq，用于计算离线消息 |
| `is_top` | TINYINT | 否 | 0 | 是否置顶：1 是 / 0 否 |
| `top_time` | DATETIME | 是 | NULL | 置顶时间 |
| `is_muted` | TINYINT | 否 | 0 | 是否免打扰：1 是 / 0 否 |
| `is_deleted` | TINYINT | 否 | 0 | 本端是否隐藏该会话：1 隐藏 / 0 显示 |
| `at_flag` | TINYINT | 否 | 0 | 是否有未读的 @ 提醒：1 有 / 0 无 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

**索引：**

| 索引名 | 类型 | 列 | 用途 |
|---|---|---|---|
| `PRIMARY` | 主键 | `id` | 主键定位 |
| `uk_conv_user` | 唯一 | `conversation_id, user_id` | 一个用户在一个会话里只有一行状态 |
| `idx_user_deleted` | 普通 | `user_id, is_deleted` | 会话列表主查询路径：按用户过滤未隐藏会话，再按置顶与最新消息排序 |

> **`last_ack_seq` 的作用**：记录用户「已读到哪条消息的 seq」。上线拉取离线消息时，只需查 `im_message` 中 `conversation_id=? AND seq > last_ack_seq` 的记录，避免全量扫描。

---

## 七、消息模块（im-message）

### 7.1 `im_message` — 消息表（兼离线消息持久化队列）

系统数据量最大的表。每条消息一行，`content` 存正文或附件文件 ID，`extra` 存结构化扩展信息（JSON）。它同时充当**离线消息队列**——用户不在线时消息落库，上线后按 seq 补拉。

| 字段 | 类型 | 允许空 | 默认值 | 含义 |
|---|---|---|---|---|
| `id` | BIGINT | 否 | — | 消息 ID（雪花） |
| `client_msg_id` | VARCHAR(64) | 否 | — | 客户端消息 ID，与发送者一起构成幂等键 |
| `conversation_id` | BIGINT | 否 | — | 所属会话 ID |
| `from_user_id` | BIGINT | 否 | — | 发送者 ID，**0 表示系统通知** |
| `msg_type` | TINYINT | 否 | — | 类型：1 文本 / 2 图片 / 3 文件 / 4 语音 / 5 系统通知 |
| `content` | TEXT | 是 | — | 消息内容：文本为正文，附件类为文件 ID |
| `extra` | JSON | 是 | NULL | 扩展信息：附件元数据、@ 列表等（见[扩展说明](#十扩展说明)） |
| `seq` | BIGINT | 否 | — | 会话内自增序列，游标分页与未读计算依据 |
| `is_recalled` | TINYINT | 否 | 0 | 是否已撤回：1 是 / 0 否 |
| `recall_time` | DATETIME | 是 | NULL | 撤回时间 |
| `send_time` | DATETIME | 否 | — | 发送时间（服务端落库时间） |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP | 创建时间 |

**索引：**

| 索引名 | 类型 | 列 | 用途 |
|---|---|---|---|
| `PRIMARY` | 主键 | `id` | 主键定位 |
| `uk_from_client` | 唯一 | `from_user_id, client_msg_id` | **幂等核心**：同一客户端消息 ID 只落库一次，网络重试重复提交触发唯一键冲突 |
| `idx_conv_seq` | 普通 | `conversation_id, seq` | 历史消息游标分页：`WHERE conversation_id=? AND seq<? ORDER BY seq DESC` |
| `idx_send_time` | 普通 | `send_time` | 按时间范围查询/清理 |

> **撤回是软状态**：消息撤回不删行，而是置 `is_recalled=1` + `recall_time`，前端渲染为「xx 撤回了一条消息」，保留可追溯性。

### 7.2 `im_message_read` — 消息送达/已读回执表

记录每个接收者对每条消息的送达与已读时间，支撑「已读/未读」显示与群消息回执统计。

| 字段 | 类型 | 允许空 | 默认值 | 含义 |
|---|---|---|---|---|
| `id` | BIGINT | 否 | — | 主键 |
| `message_id` | BIGINT | 否 | — | 消息 ID |
| `user_id` | BIGINT | 否 | — | 接收者 ID |
| `delivered_time` | DATETIME | 是 | NULL | 送达时间 |
| `read_time` | DATETIME | 是 | NULL | 已读时间 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP | 创建时间 |

**索引：**

| 索引名 | 类型 | 列 | 用途 |
|---|---|---|---|
| `PRIMARY` | 主键 | `id` | 主键定位 |
| `uk_msg_user` | 唯一 | `message_id, user_id` | 一条消息对一个接收者只有一行回执 |
| `idx_user_msg` | 普通 | `user_id, message_id` | 按用户维度批量更新已读状态 |

### 7.3 `im_message_delete` — 消息单端删除表

「删除」消息只对本人生效（类似微信「删除聊天记录」不影响对方）。一行代表「某用户删除了某消息」，查询时对本端做左连接过滤。

| 字段 | 类型 | 允许空 | 默认值 | 含义 |
|---|---|---|---|---|
| `id` | BIGINT | 否 | — | 主键 |
| `message_id` | BIGINT | 否 | — | 消息 ID |
| `user_id` | BIGINT | 否 | — | 执行删除的用户 ID |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP | 删除时间 |

**索引：**

| 索引名 | 类型 | 列 | 用途 |
|---|---|---|---|
| `PRIMARY` | 主键 | `id` | 主键定位 |
| `uk_msg_user` | 唯一 | `message_id, user_id` | 防止同一用户对同一消息重复删除 |
| `idx_user_msg` | 普通 | `user_id, message_id` | 拉取消息列表时按用户过滤已删除项 |

> **`im_message_read` 与 `im_message_delete` 的区别**：前者是「我看过了」（状态回执），后者是「我不再看到它」（单端隐藏）。二者互不影响，且都不动 `im_message` 主表。

---

## 八、群组模块（im-group）

### 8.1 `im_group` — 群组表

群聊主体，一行一个群。

| 字段 | 类型 | 允许空 | 默认值 | 含义 |
|---|---|---|---|---|
| `id` | BIGINT | 否 | — | 群 ID |
| `name` | VARCHAR(64) | 否 | — | 群名称 |
| `avatar` | VARCHAR(512) | 是 | NULL | 群头像 |
| `notice` | VARCHAR(512) | 是 | NULL | 群公告 |
| `owner_id` | BIGINT | 否 | — | 群主用户 ID |
| `max_member` | INT | 否 | 200 | 最大成员数 |
| `mute_all` | TINYINT | 否 | 0 | 全员禁言：1 是 / 0 否 |
| `status` | TINYINT | 否 | 1 | 状态：1 正常 / 0 已解散 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |
| `deleted` | TINYINT | 否 | 0 | 逻辑删除 |

**索引：**

| 索引名 | 类型 | 列 | 用途 |
|---|---|---|---|
| `PRIMARY` | 主键 | `id` | 主键定位 |
| `idx_owner` | 普通 | `owner_id` | 查「某用户创建/管理的群」 |

> 群主权限用 `owner_id` 直接判定，无需唯一键约束。

### 8.2 `im_group_member` — 群成员表

一行一成员。**退群置 `status=0` 而非物理删除**，重新入群时复用同一行，以保留入群历史与群内昵称等设置。

| 字段 | 类型 | 允许空 | 默认值 | 含义 |
|---|---|---|---|---|
| `id` | BIGINT | 否 | — | 主键 |
| `group_id` | BIGINT | 否 | — | 群 ID |
| `user_id` | BIGINT | 否 | — | 成员用户 ID |
| `role` | TINYINT | 否 | 3 | 群内角色：1 群主 / 2 管理员 / 3 普通成员 |
| `nickname_in_group` | VARCHAR(32) | 是 | NULL | 群内昵称 |
| `is_muted` | TINYINT | 否 | 0 | 是否被单独禁言：1 是 / 0 否 |
| `mute_end_time` | DATETIME | 是 | NULL | 禁言结束时间，为空表示无限期 |
| `join_time` | DATETIME | 否 | CURRENT_TIMESTAMP | 入群时间 |
| `status` | TINYINT | 否 | 1 | 状态：1 在群 / 0 已退群/被移除 |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

**索引：**

| 索引名 | 类型 | 列 | 用途 |
|---|---|---|---|
| `PRIMARY` | 主键 | `id` | 主键定位 |
| `uk_group_user` | 唯一 | `group_id, user_id` | 一个用户在一个群里只有一行 |
| `idx_user_status` | 普通 | `user_id, status` | 查「我加入的群列表」（按在群状态过滤） |

> **群成员与好友关系的删除策略相反**：好友表物理删除（避免唯一键挡住重新加好友），群成员用 `status` 软删除（`uk_group_user` 唯一键下，复用旧行而非插新行，天然不冲突且保留历史）。

---

## 九、文件模块（im-file）

### 9.1 `im_file` — 文件元数据表

只存**元数据**，真实文件字节在存储层（本地磁盘或 MinIO，由 `im.file.storage` 配置切换）。消息里的附件、用户头像都指向这张表的一行。

| 字段 | 类型 | 允许空 | 默认值 | 含义 |
|---|---|---|---|---|
| `id` | BIGINT | 否 | — | 文件 ID |
| `uploader_id` | BIGINT | 否 | — | 上传者用户 ID |
| `biz_type` | VARCHAR(32) | 否 | — | 业务类型：avatar / chat_image / chat_file / chat_voice |
| `storage_type` | VARCHAR(16) | 否 | `local` | 存储实现：local / minio |
| `bucket` | VARCHAR(64) | 是 | NULL | 存储桶，本地实现为空 |
| `object_key` | VARCHAR(512) | 否 | — | 对象键 / 相对路径 |
| `original_name` | VARCHAR(255) | 是 | NULL | 原始文件名 |
| `url` | VARCHAR(1024) | 是 | NULL | 访问地址（受控下载地址或预签名直链） |
| `size` | BIGINT | 否 | 0 | 文件字节数 |
| `content_type` | VARCHAR(128) | 是 | NULL | MIME 类型 |
| `ext` | VARCHAR(32) | 是 | NULL | 扩展名，不含点 |
| `md5` | VARCHAR(64) | 是 | NULL | 文件 MD5，用于秒传 |
| `duration` | INT | 是 | NULL | 语音时长（秒） |
| `create_time` | DATETIME | 否 | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | DATETIME | 否 | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |
| `deleted` | TINYINT | 否 | 0 | 逻辑删除 |

**索引：**

| 索引名 | 类型 | 列 | 用途 |
|---|---|---|---|
| `PRIMARY` | 主键 | `id` | 主键定位 |
| `idx_md5` | 普通 | `md5` | **秒传**：上传前按 MD5 查是否已存在相同文件，命中则直接复用 |
| `idx_uploader` | 普通 | `uploader_id, create_time` | 查「我上传的文件」列表，按时间排序 |

> **存储抽象**：`storage_type` + `bucket` + `object_key` 三字段组合，使同一张表能同时描述本地文件（`storage_type=local`，`bucket` 为空，`object_key` 为相对路径）与 MinIO 对象（`storage_type=minio`，三者齐全）。切换存储实现无需改表结构。

---

## 十、扩展说明

### 10.1 `im_message.extra` JSON 字段结构

`extra` 列为 `JSON` 类型，映射到 `com.im.common.domain.MessageExtra`（`@JsonInclude(NON_NULL)`，只序列化非空字段）。它把「附件元数据」「@ 提醒」「系统通知动作」等非通用信息从主表列中解耦出来，避免为每种消息类型都加一堆稀疏列。

| JSON 键 | Java 类型 | 含义 |
|---|---|---|
| `fileId` | Long | 关联文件 ID（指向 `im_file.id`） |
| `fileName` | String | 文件原始名 |
| `fileUrl` | String | 文件访问地址 |
| `fileSize` | Long | 文件字节数 |
| `contentType` | String | MIME 类型 |
| `ext` | String | 扩展名，不含点 |
| `width` | Integer | 图片宽度（像素） |
| `height` | Integer | 图片高度（像素） |
| `duration` | Integer | 语音时长（秒） |
| `atUserIds` | List\<Long\> | 被 @ 的用户 ID 列表 |
| `atAll` | Boolean | 是否 @ 全员 |
| `action` | String | 系统通知动作标识，前端据此决定交互 |

**按消息类型使用不同的键子集：**

- **文本消息（type=1）**：`extra` 通常为 NULL；群聊里若 @ 了人，则填 `atUserIds` / `atAll`。
- **图片消息（type=2）**：`content` 存文件 ID，`extra` 填 `fileId` / `fileName` / `fileUrl` / `width` / `height`。
- **文件消息（type=3）**：`extra` 填 `fileId` / `fileName` / `fileUrl` / `fileSize` / `contentType` / `ext`。
- **语音消息（type=4）**：`extra` 填 `fileId` / `fileUrl` / `duration`。
- **系统通知（type=5）**：`from_user_id=0`，`extra` 填 `action`（如 `recall`、`join_group`）供前端识别交互类型。

> `MessageExtra.hasMention()` 方法：当 `atAll=true` 或 `atUserIds` 非空时返回 true，供会话模块判断是否需要**打破免打扰**（即使会话设了 `is_muted`，被 @ 时仍提醒）。

### 10.2 关键设计模式速查

| 模式 | 落地位置 | 解决的问题 |
|---|---|---|
| **幂等键** | `im_message.uk_from_client`、`im_conversation.uk_biz_key` | 网络重试/重复提交不产生重复数据 |
| **虚拟生成列 + 唯一索引** | `im_friend_request.pending_flag` | 「唯一约束」与「保留历史」二者兼得 |
| **双向冗余** | `im_friend`（两行） | 好友双方各自独立维护备注/分组/拉黑 |
| **软状态 vs 物理删除** | 群成员 `status` / 好友物理删 | 按「是否需要保留历史」分别选择 |
| **冗余摘要** | `im_conversation.last_msg_*` | 会话列表页免 join 消息大表 |
| **游标分页** | `im_message.idx_conv_seq`（seq 游标） | 海量消息深翻页不用 OFFSET，性能稳定 |
| **存储抽象** | `im_file.storage_type/bucket/object_key` | 本地/MinIO 双实现共用一张表 |
| **单端删除表** | `im_message_delete` | 「删除」只影响本人，不动主表与他人视图 |

### 10.3 种子数据（`im_data.sql`）

初始化脚本内置一套可直接登录的演示数据（**密码统一 `123456`**，Argon2id 密文离线生成）：

| 账号 | userId | 角色 | 说明 |
|---|---|---|---|
| `admin` | 1000 | 超级管理员 | 拥有全部 7 个权限点 |
| `alice` | 1001 | 普通用户 | 与 bob 互为好友 |
| `bob` | 1002 | 普通用户 | 与 alice 已有演示单聊会话 |

同时预置：alice↔bob 的好友关系（双向两行）、一条已同意的历史申请、一个单聊会话（`biz_key=s:1001:1002`）、3 条演示消息（seq 1~3）、以及送达/已读回执（bob 已读完 seq 1、2，seq 3 尚未被 alice 读取，制造 1 条未读用于演示红点）。

> 种子数据脚本可重复执行——每次 INSERT 前先 `DELETE` 清理相关表。

### 10.4 表数量小结

- **物理表 15 张**，分布于 6 个模块：用户权限 5、好友 2、会话 2、消息 3、群组 2、文件 1。
- 主表（用户、角色、权限、会话、群组、文件）启用逻辑删除 `deleted`；关系表与消息表按业务特性选用物理删除或软状态位。
- 全库无物理外键，关系由应用层与索引维护，符合互联网应用「弱化外键、便于水平拆分」的通行实践。
