# ==============================================================================
# 构建产物
# ==============================================================================
target/
!.mvn/wrapper/maven-wrapper.jar
build/
out/
*.jar
*.war
*.ear
*.dll
*.so
*.dylib
*.pak
*.exe
*.txt
# 打包产物需要发布时单独用 git add -f，不靠忽略规则例外放行

# ==============================================================================
# 前端
# ==============================================================================
node_modules/
im-ui/dist/
im-ui/dist-ssr/
.npm/
.pnpm-store/
*.local
.vite/

# Electron 桌面端：只忽略构建产物 / 依赖 / 大二进制，源码要提交
# 保留提交：main.js、preload.js、package.json、package-lock.json、bin/ffmpeg放置说明.txt
# （node_modules、ffmpeg.exe 其实已被全局规则 node_modules/、*.exe 覆盖，这里显式列出更直观）
electron/node_modules/
electron/dist/
electron/release/
electron/bin/ffmpeg.exe

# ==============================================================================
# 运行时产生的文件
# ==============================================================================
logs/
*.log
# 本地文件存储模式的上传目录（默认在 ${user.home}/im-files，不在仓库内；
# 若把 IM_FILE_DIR 指到了项目里，这里兜住）
im-files/

# ==============================================================================
# 环境与密钥
# 凭据只从环境变量读取，任何 .env 都不应进版本库
# ==============================================================================
.env
.env.*
!.env.example
*.pem
*.p12
*.jks

# ==============================================================================
# IDE / 编辑器
# ==============================================================================
.idea/
*.iml
*.ipr
*.iws
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.settings/
.classpath
.project
.factorypath
*.swp
*.swo
*~

# ==============================================================================
# 操作系统
# ==============================================================================
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini

# ==============================================================================
# 临时调试产物
# 接口串测时很容易在根目录落下一堆 json / token 快照，统一忽略掉
# ==============================================================================
_probe/
tmp/
temp/
*.tmp
*.bak
*.orig
